package org.tzi.use.plugins.jacamo.runtime;

public enum ConnectorCapability {
    FULL_SNAPSHOT, EVENT_SUBSCRIPTION, RECONNECT,
    AGENT_STATE, ARTIFACT_STATE, ORGANISATION_STATE, OPERATION_EVENTS
}
