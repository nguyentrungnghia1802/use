# Phase 5 — original Auction supported scope

Status: SUPPORTED_SCOPE_PASS; full original standalone semantics are explicitly
not claimed.

The upstream JCM hash is `c766fb0d...7d2fb`. The official generic pipeline loads
five configured agents, the official Jason program, one group, three roles, one
scheme, two missions, four organizational goals, one organizational plan and two
norms. It preserves exact group-role tuples `auctioneer 1..1` and
`participant 0..300`. The deterministic model revision is
`d144c7b8...6d4c4`; frozen V2 compiles with 562 semantic elements and 994 USE
objects. The same case passes the loopback production transport in a separate
JaCaMo/server JVM and a platform-free USE JVM.

Existing real-API component controls still prove CArtAgO operation/property
success and failure boundaries, Moise board facts, exact runtime aliases,
PRE/POST verification and resync. They remain labelled
`SUPPORTED_SUBSET_COMPLETE`; they are not substituted for the original source.
Bridge runtime observations without exact runtime-model binding remain
`EVIDENCE_ONLY` and cannot feed OCL.

The original self-referencing plan/deontic deadline/natural-language timing
equivalence is still `UNAVAILABLE`/explicitly unsupported. No NPL-to-OCL
translation and no claim of full original Auction E2E is made.
